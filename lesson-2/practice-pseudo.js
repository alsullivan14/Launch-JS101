// Add two numbers together
// Return value to user

// Instantiate variable to first item in array
// Use forEach on Array to concatenate current item starting at index 1 until the loop closes
// Return end result to user

// Create empty array
// Using for loop, iterate over every other item
// Push every other item to empty array
// Return array