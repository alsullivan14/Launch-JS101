function first() {
  return {
    prop1: "hi there"
  };
}

function second() {
  return
  {
    prop1: "hi there"
  };
}

console.log(first());
console.log(second());//will return undefined because JS places semicolon where it thinks they're appropriate(after return here)